# Server Configuration
PORT=3000
NODE_ENV=development

# AI Provider: mock | local | animegan | replicate
AI_PROVIDER=mock

# Conversion Mode: anime | video-only | both
# - anime: Sketch → Anime image (no video) - $0.005, 8-12s
# - video-only: Sketch → Video directly (preserves original drawing)
# - both: Sketch → Anime → Video
CONVERSION_MODE=anime

# Video Model: hailuo | seedance
# - hailuo: MiniMax hailuo-02-fast - 6s, 512p, $0.10/video
# - seedance: ByteDance seedance-1-lite - 4s, 480p, $0.072/video
VIDEO_MODEL=seedance

# Replicate API Token (get from https://replicate.com/account/api-tokens)
# Required when AI_PROVIDER=replicate
# Get free credits at: https://replicate.com
# REPLICATE_API_TOKEN=r8_your_token_here

# Content Moderation (optional)
ENABLE_INPUT_MODERATION=false
ENABLE_OUTPUT_MODERATION=false

# CORS (allow drawing pad and display screen)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

